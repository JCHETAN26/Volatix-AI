# Volatix-AI agents service runtime deps.
# LangGraph + langchain-core power the 3-tier graph. langchain-openai and
# langchain-ollama are both installed so LLM_PROVIDER can pick either at
# runtime; the mock provider has zero extra deps.
langgraph==0.2.60
langchain-core==0.3.27
langchain-openai==0.2.14
langchain-ollama==0.2.2
langchain-google-genai==2.0.7
pydantic==2.10.4
httpx==0.28.1
kafka-python-ng==2.2.3
psycopg2-binary==2.9.10
# Phase 6 — LLM evaluation harness. Only imported by agents.eval.*; the
# streaming consumer never touches it. Ragas brings in datasets +
# langchain extras, adding ~80MB to the agents image but keeping the
# eval runner in the same container as the agent code it tests.
ragas==0.2.10
datasets==3.2.0
